# 이래저래 (ElhaeJulhae) v2 - 지식정보

**작성일**: 2026-09-13  
**프로젝트**: ElhaeJulhae_v2 (캘린더 기반 임신준비 부부 앱)  
**기획 근거**: `상세기획서.md`

---

## 📌 1. 기획서 핵심 요약

### 1.1 앱의 정체성
- **주요 기능**: 캘린더/스케줄러 기반 투두 관리
- **차별점**: 짝꿍 실시간 알림으로 서로의 감정적 부담을 공유하고 지원
- **목적**: 임신준비 부부가 함께 루틴을 만들고 감정적 부담을 나누기

### 1.2 온보딩 프로세스 (4단계)
```
Step 1: 개인정보 입력 (필수)
  └─ 성별, 생년월일, 임신준비 단계 (자연임신/인공수정/체외수정)

Step 2: 짝꿍정보 (선택)
  └─ 싱글/커플 모드 선택 → 짝꿍 정보 입력 또는 초대코드 입력

Step 3: 메인 D-Day 설정 (필수)
  └─ 배란일(자연) / 시술일(인공수정) / 이식일(체외수정)

Step 4: 캘린더 뷰 선택
  └─ Month/Week/Day 중 기본 뷰 선택 → MainPage로 이동
```

### 1.3 모드 전환
- 싱글 ↔ 커플 모드 언제든 전환 가능 (설정에서)
- 데이터 손실 없이 전환

### 1.4 캘린더 뷰 (3종류)

#### A. 월 뷰 (Month View)
- 각 날짜에 투두 개수 표시
- 메인 D-Day 특별 강조
- 날짜 클릭 → 일 뷰로 이동

#### B. 주 뷰 (Week View)
- 월~일 요일별 투두 리스트 표시

#### C. 일 뷰 (Day View)
- 현재 날짜 + D-Day 오프셋 (예: "배란일 -3일") 표시
- "오늘의 할 일" 섹션 (공통/내 투두)
- "준비 과정 투두" 섹션

### 1.5 투두 관리 규칙
| 항목 | 설명 |
|------|------|
| **텍스트** | 투두 내용 |
| **날짜** | ⭐ **필수** — 모든 투두는 날짜 지정 필수 |
| **반복** | 선택사항 → **Routine 타입으로 분리** (반복 투두는 Routine 사용) |
| **카테고리** | **페이지 분류 기반** — 사용자가 현재 보고 있는 탭에서 자동 결정<br>예: "공통 탭"에서 추가 → "공통" / "내 투두 탭"에서 추가 → "내 투두" |
| **완료** | 체크 표시 후 +10 EXP 획득, 짝꿍과 동기화 |
| **삭제** | 소프트 삭제(휴지통), 완전 삭제 가능 |

### 1.6 짝꿍 실시간 알림 (차별점 기능)
```
Flow:
  A가 "짝꿍 투두" 탭에서 투두 옆 🔔 버튼 클릭
    ↓
  B의 화면에 즉시 토스트 알림 표시
    ↓
  알림 내용: "A가 '[투두명]' 확인해달라고 했어요! 🔔"
    ↓
  B가 해당 투두 확인 및 처리 가능

💡 의도: "넛지" - 상대방의 해야할 일을 계속 상기시키기
⭐ 중요: 실시간 전송 필수 (Firebase로 구현)
```

### 1.7 캐릭터/레벨 시스템
```
표시 위치: 내 정보 페이지 > 하단

구성:
  ├─ Lv. [현재 레벨]
  ├─ 경험치 프로그레스 바 (0~100)
  └─ "다음 레벨까지 남은 EXP" 표시

계산:
  - 투두 완료: +10 EXP
  - 100 EXP: 1 레벨업 (nextLevelExp 항상 100)
```

### 1.8 D-Day 기반 추천 루틴 시스템
D-Day를 클릭하면 "준비할 루틴 리스트" 표시 (**비활성화 상태로 추천**):

#### 루틴 템플릿 (모든 사람 공통, 매일)
사용자가 선택하면 기본값이 설정된 상태로 제공:
- 영양제 복용
- 운동 30분
- 스트레스 관리

#### 이벤트성 루틴 (단계별, 비활성화 상태 추천)

**자연임신 (배란일 중심)**
- 배란 테스트기 사용
- 기초체온 측정

**인공수정 (시술일 중심)**
- 시술 전: 난포천자 주사(여), 정액 채취 준비(남)
- 시술 후: 황체호르몬 투여 등

**체외수정 (이식일 중심)**
- 배란유도제 주사(여), 정액 채취 준비(남)
- 난자 체취(여)
- 배아 등급 확인
- 이식 준비
- 착상 대기 기간 관리

**구현 노트**: 복잡도가 높으면 먼저 기본 투두 관리만 하고 나중에 개선

### 1.9 화면 흐름 & 네비게이션
```
OnboardingPage
  ├─ Step 1: 개인정보
  ├─ Step 2: 짝꿍정보
  ├─ Step 3: D-Day 설정
  └─ Step 4: 뷰 선택
       ↓ (완료)
       
MainPage
  ├─ Header
  │  ├─ D-Day 카운트다운 표시
  │  └─ 메뉴 버튼 (≡)
  │
  ├─ CalendarView (Month/Week/Day)
  │  └─ 투두 표시 + 상호작용
  │
  ├─ TodoList 탭
  │  ├─ 공통
  │  ├─ 내 투두
  │  └─ 짝꿍 투두
  │
  └─ Menu (≡)
     ├─ 내 정보
     │  ├─ 개인정보 표시
     │  └─ 캐릭터 (Lv. + EXP 바)
     └─ 환경설정
        ├─ 모드 변경 (싱글 ↔ 커플)
        └─ D-Day 수정
```

### 1.10 개발 단계별 구현 순서 (5 Phase)

| Phase | 내용 | 범위 |
|-------|------|------|
| **Phase 1** | 기본 구조 (로컬) | 온보딩(Step1~3), MainPage 레이아웃, 월 뷰, 로컬 투두 추가/완료/삭제 |
| **Phase 2** | Firebase 연동 | Firebase 설정, 투두 실시간 동기화, 커플 모드 정상화 |
| **Phase 3** | 짝꿍 알림 | 🔔 알림 기능, 실시간 토스트 메시지 |
| **Phase 4** | 확장 기능 | 주/일 뷰, D-Day 준비 투두 시스템, 반복 투두 설정 |
| **Phase 5** | 완성 & 폴리싱 | 캐릭터 시스템, 내 정보 페이지, 설정 페이지 |

---

## 📊 2. 데이터 구조

### 🆕 투두 vs 루틴 개념
| 구분 | 투두 | 루틴 |
|------|------|------|
| **날짜** | 단일 (필수) | 범위 (startDate~endDate) |
| **반복** | 없음 | 있음 (daily/weekly/biweekly/monthly) |
| **예시** | "병원 방문" (2026-09-15) | "영양제 복용" (매일, 06:00+12:00) |
| **저장** | DB | DB (정의만) |
| **렌더링** | DB에서 직접 | 메모리에서 동적 생성 |
| **완료 추적** | Todo.completed | TodoCompletion 테이블 |

---

### 🆕 2.0 OnboardingProgress (온보딩 진행 상태 추적) - E2
```typescript
interface OnboardingProgress {
  id: string                          // 고유 ID (사용자별 1개)
  
  // Step 1: 개인정보
  step1?: {
    gender: 'female' | 'male'
    birthDate: string
    stage: 'natural' | 'artificial' | 'ivf'
    completedAt: string
  }
  
  // Step 2: 짝꿍정보
  step2?: {
    mode: 'single' | 'couple'
    partner?: {
      gender: 'female' | 'male'
      birthDate: string
    }
    completedAt: string
  }
  
  // Step 3: D-Day 설정
  step3?: {
    type: 'natural' | 'artificial' | 'ivf'
    targetDate: string
    completedAt: string
  }
  
  // Step 4: 뷰 선택 (선택사항)
  step4?: {
    selectedView: 'month' | 'week' | 'day'  // 기본값: 'month'
    completedAt: string
  }
  
  createdAt: string
  deletedAt?: string
}

// 용도: 온보딩 중단 후 재방문 시 진행 상태 복구
```

---

### 2.1 User (사용자 정보)
```typescript
interface User {
  id: string                          // 고유 ID (Firebase Auth)
  gender: 'female' | 'male'          // 성별
  birthDate: string                   // 생년월일 (ISO 8601)
  stage: 'natural' | 'artificial' | 'ivf'  // 임신준비 단계
  role: 'owner' | 'partner'          // 역할 (초대자/초대받은자)
}
```

### 2.2 Partner (짝꿍 정보 — 간소화)
```typescript
interface Partner {
  gender: 'female' | 'male'          // 성별
  birthDate: string                   // 생년월일 (ISO 8601)
  // ⚠️ 이름/연락처 미포함 (코드/URL 초대 방식으로 개인정보 최소화)
}
```

### 2.3 Todo (단일 날짜 투두)
```typescript
interface Todo {
  id: string                          // 고유 ID
  text: string                        // 투두 텍스트
  date: string                        // ⭐ 필수 — 실행 예정 날짜 (ISO 8601, 단일)
  category: 'common' | 'mine' | 'partner'  // 카테고리 (공통/내것/짝꿍것)
  assignedTo?: 'owner' | 'partner'   // 담당자 (선택)
  completed: boolean                  // 완료 여부
  completedAt?: string                // 완료 시각 (ISO 8601)
  createdAt: string                   // 생성 시각
  deletedAt?: string                  // 삭제 시각 (소프트 삭제)
  // ❌ repeat 필드 제거됨 → Routine으로 이전
}
```

### 2.4 🆕 Routine (반복 투두)
```typescript
interface Routine {
  id: string                          // 고유 ID
  text: string                        // 루틴 텍스트 (예: "영양제 복용")
  category: 'common' | 'mine' | 'partner'  // 카테고리
  assignedTo?: 'owner' | 'partner'   // 담당자 (선택)
  
  // 반복 규칙
  repeat: 'daily' | 'weekly' | 'biweekly' | 'monthly'  // 반복 유형
  
  // 반복 요일 (E7: weekly/biweekly 선택 시 선택 사항)
  dayOfWeek?: number                  // 0-6 (0=일, 1=월, ..., 6=토)
  // 예: startDate="2026-09-18" (수요일), dayOfWeek=1 (월요일)
  //     → 매주 월요일 반복 (startDate와 무관)
  
  // 시간 설정 (선택, Phase 3+ 자동 알림 대비)
  time?: string                       // 단일 시간 (예: "06:00")
  times?: string[]                    // 다중 시간 (예: ["06:00", "12:00", "18:00"])
  
  // 유효 기간
  startDate: string                   // 루틴 시작 날짜 (ISO 8601)
  endDate?: string                    // 루틴 종료 날짜 (없으면 무한)
  
  createdAt: string                   // 생성 시각
  deletedAt?: string                  // 삭제 시각 (소프트 삭제, E10)
}
```

### 2.5 🆕 TodoCompletion (루틴/반복 투두 완료 추적)
```typescript
interface TodoCompletion {
  id: string                          // 고유 ID (추적용)
  routineId: string                   // 어느 루틴의 완료인지
  date: string                        // 완료 날짜 (ISO 8601)
  completed: boolean                  // 완료 여부
  completedAt?: string                // 완료 시각
  createdAt: string                   // 기록 생성 시각
  
  // 용도: 동일 루틴의 여러 날짜 완료 상태 각각 추적
  // 예: routineId="routine_001" (영양제)의 2026-09-18, 2026-09-20, ... 완료 상태
}
```

### 2.6 DDay (메인 D-Day 및 추천 투두)
```typescript
interface DDay {
  id: string                          // 고유 ID
  type: 'natural' | 'artificial' | 'ivf'  // D-Day 타입
  targetDate: string                  // 목표 날짜 (배란일/시술일/이식일) (ISO 8601)
  name: string                        // D-Day 이름 ('배란일' | '시술일' | '이식일')
  suggestedTodos?: Todo[]             // 추천 투두 리스트 (이벤트 기반)
  suggestedRoutines?: Routine[]       // 추천 루틴 리스트 (루틴 기반)
}
```

### 2.7 Character (캐릭터/레벨 정보)
```typescript
interface Character {
  level: number                       // 현재 레벨 (정수)
  currentExp: number                  // 현재 EXP (0~100, 순환)
  totalExp: number                    // 누적 EXP (총합, 계속 증가)
  nextLevelExp: number                // 다음 레벨 필요 EXP (항상 100)
}
```

### 🆕 2.8 RoutineTemplate (D-Day 루틴 추천 템플릿)
```typescript
interface RoutineTemplate {
  id: string                          // 템플릿 ID
  text: string                        // 루틴 텍스트 (예: "영양제 복용")
  category: 'common' | 'mine' | 'partner'  // 카테고리
  repeat: 'daily' | 'weekly' | 'biweekly' | 'monthly'  // 반복 규칙
  time?: string | string[]            // 기본 시간 설정 (예: ["06:00", "12:00"])
  stage: 'natural' | 'artificial' | 'ivf'  // 어느 단계에 추천되는가
  daysOffset?: {                      // D-Day 기준 날짜 범위
    start: number                     // 시작 (예: -5 = D-Day 5일 전)
    end: number                       // 종료 (예: +5 = D-Day 5일 후)
  }
}

// 사용법:
// 1. D-Day 화면에서 RoutineTemplate 목록 표시 (체크박스 비활성화)
// 2. 사용자가 체크박스 클릭
// 3. 루틴 편집 모달 열림 (template 값들 기본값으로 입력된 상태)
// 4. 사용자가 필요시 수정 후 저장
// 5. Routine 테이블에 새로운 Routine 레코드 생성
```

---

### 🔄 투두 vs 루틴 렌더링 방식 (하이브리드)

#### Step 1: 데이터 로드 (DB)
```
- Routine: "영양제 복용" (daily, startDate=2026-09-01, endDate=무한, time=06:00)
```

#### Step 2: 동적 투두 생성 (메모리)
```
캘린더 뷰: 2026-09-18 ~ 2026-09-25
→ repeat='daily'이므로 7개 투두 동적 생성 (메모리상)

[
  { id: "routine_001_2026-09-18", text: "영양제 복용", date: "2026-09-18", completed: false },
  { id: "routine_001_2026-09-19", text: "영양제 복용", date: "2026-09-19", completed: false },
  ...
]
```

#### Step 3: 완료 상태 조회 (DB)
```
TodoCompletion에서 조회:
- WHERE routineId="routine_001" AND date BETWEEN '2026-09-18' AND '2026-09-25'
- 반환: [{ date: "2026-09-18", completed: true }, { date: "2026-09-20", completed: true }]
```

#### Step 4: UI 병합 (렌더링)
```
2026-09-18: [✓] 영양제 복용     ← completed=true (DB에서 조회)
2026-09-19: [ ] 영양제 복용
2026-09-20: [✓] 영양제 복용     ← completed=true
2026-09-21: [ ] 영양제 복용
...
```

#### 🎯 장점
- **DB 최소화**: 루틴 정의만 저장 (개별 일자별 투두 미저장)
- **유연성**: 범위 변경 시 동적 계산 가능
- **성능**: 대량 반복 투두도 효율적 처리

---

## 🔐 3. Firebase 설정 항목 (Phase 2+)

### 3.1 상태
- ⚠️ **현재 `ElhaeJulhae_v2`에는 Firebase 설정이 없음**
- Firebase 연동은 **Phase 2**부터 시작
- v1 프로젝트와는 별도의 **새 Firebase 프로젝트** 생성 필요 (사용자가 Firebase 콘솔에서 직접 생성)
- **Phase 1은 로컬 localStorage만 사용** (Firebase 미필요)

### 3.2 필요한 환경 변수 (.env) — Phase 2 진입 시 생성

```env
# Firebase Configuration (from Firebase Console)
VITE_FIREBASE_API_KEY=<your_api_key>
VITE_FIREBASE_AUTH_DOMAIN=<your_auth_domain>
VITE_FIREBASE_PROJECT_ID=<your_project_id>
VITE_FIREBASE_STORAGE_BUCKET=<your_storage_bucket>
VITE_FIREBASE_MESSAGING_SENDER_ID=<your_messaging_sender_id>
VITE_FIREBASE_APP_ID=<your_app_id>
```

### 3.3 Firebase 필요 기능

| 기능 | 설명 | 용도 | Phase |
|------|------|------|-------|
| **Authentication** | 익명 인증 | 사용자 식별 (v1 참고) | Phase 2 |
| **Firestore** | NoSQL DB | User, Partner, Todo, Routine, TodoCompletion, DDay, Character 동기화 | Phase 2 |
| **Realtime Listener** | Firestore Listener | 실시간 투두 동기화 (A ↔ B) | Phase 2 |
| **Cloud Messaging** (FCM) 또는 **Firestore Listener** | 실시간 푸시 | 짝꿍 알림 (🔔) | Phase 3 |

### 3.4 Firestore 컬렉션 구조 (계획)
*Phase 2에서 상세화, 현재는 기본 구조만*

```
rooms/
  └─ {roomId}/  # 커플 연결 관계
      ├─ users: [ownerUserId, partnerUserId]
      ├─ createdAt: timestamp
      └─ dday: targetDate
      
users/
  └─ {userId}/
      ├─ profile: { gender, birthDate, stage, role }
      ├─ partner: { gender, birthDate }  # 커플 모드일 때만
      ├─ character: { level, currentExp, totalExp, nextLevelExp }
      └─ settings: { roomId, mode, ... }

todos/
  └─ {todoId}/
      ├─ text, date, category, completed, createdAt, deletedAt
      └─ (단일 날짜 투두)

routines/
  └─ {routineId}/
      ├─ text, repeat, startDate, endDate, time/times, category
      └─ (반복 투두 정의)

todoCompletions/
  └─ {completionId}/
      ├─ routineId, date, completed, completedAt
      └─ (루틴별 날짜별 완료 추적)

routineTemplates/  # D-Day 추천 루틴 (고정 데이터)
  └─ {templateId}/
      ├─ text, category, repeat, time/times
      ├─ stage (natural/artificial/ivf)
      └─ daysOffset (D-Day 기준 범위)

notifications/  # Phase 3
  └─ {notificationId}/
      ├─ from, to, message, timestamp
      └─ (실시간 알림 기록)
```

### 3.5 package.json 의존성
- Phase 1: `firebase` 미필요
- Phase 2: `npm install firebase@^10.14.0` 추가

---

## 🔍 4. v1(ElhaeJulhae)과의 관계 및 참고사항

### 4.1 v1의 상태
- **기술 스택**: React + Firebase Firestore + 익명 인증
- **UI 구조**: 탭 기반 (공통/내 투두/짝꿍 투두)
- **알려진 문제**: "UI 반응성 이슈", "탭 클릭 등 기본 기능 문제" (버그)
- **파일 위치**: `C:\Users\Lap\workspaces\ElhaeJulhae\`

### 4.2 v2 개발 전략
- **UI 재설계**: v1의 탭 기반 → v2의 캘린더 기반으로 완전히 새로 구성
- **로직 참고**: Firestore 동기화, 익명 인증 등 v1의 검증된 로직은 참고 가능 (코드 이식 아님)
- **코드 재사용**: 권장하지 않음 — UI 버그를 피하기 위해 v2는 처음부터 새로 작성
- **.env 구조**: v1의 `VITE_FIREBASE_*` 환경변수 이름 규칙 따를 것

### 4.3 v1에서 배운 교훈
- Firebase 스키마 설계의 중요성 (사전에 잘 구조화된 컬렉션 필요)
- 동기화 전략 필요 (v1에서 사용한 `hasPendingWrites` 등 안티-에코 패턴 검토)
- UI 반응성은 초기 설계부터 고려 필수 (컴포넌트 분리, 상태 관리 구조화)

---

## ✅ 5. 확인 및 준비사항

### 5.1 필수 요구사항 (기획서 체크리스트)
- ✅ 캘린더/스케줄러가 메인 기능
- ✅ 날짜 지정 필수 (투두 규칙)
- ✅ 짝꿍 실시간 알림 필수 (Firebase)
- ✅ D-Day 기반 투두 관리
- ✅ 간단한 레벨 표시 (캐릭터 시스템)
- ✅ 싱글/커플 모드 전환 가능

### 5.2 현재 프로젝트 상태
- ✅ Vite + React 19 + TypeScript 5.3 스캐폴드 준비 완료
- ✅ `상세기획서.md` 작성 완료
- ✅ `지식정보.md` 작성 완료 (데이터 구조 확정)
- ✅ `종합검토_최종분석.md` 작성 완료 (시나리오 검토)
- ✅ `최종결정사항_반영.md` 작성 완료 (6개 의사결정)
- ⏳ 코드 구현 미시작 (Phase 1 준비)
- ⏳ Firebase 미필요 (Phase 2 대기)
- ⏳ `.env` 미생성 (Phase 2 진행 시 생성)

### 5.3 Phase 1 구현 준비 사항

#### A. 프로젝트 구조
```
src/
├── components/
│   ├── OnboardingPage/
│   │   ├── Step1.tsx (개인정보)
│   │   ├── Step2.tsx (짝꿍정보)
│   │   ├── Step3.tsx (D-Day)
│   │   └── OnboardingContainer.tsx
│   ├── MainPage/
│   │   ├── Header.tsx (D-Day 카운트다운)
│   │   ├── CalendarView/ (Month 뷰만 Phase 1)
│   │   ├── TodoList.tsx (공통/내 투두 탭)
│   │   └── MainContainer.tsx
│   └── common/ (재사용 컴포넌트)
├── pages/
│   ├── Onboarding.tsx
│   └── Main.tsx
├── types/
│   └── index.ts (User, Partner, Todo, Routine, TodoCompletion, DDay, Character)
├── utils/
│   ├── storage.ts (localStorage 관리)
│   ├── routine.ts (동적 투두 생성 로직)
│   └── calendar.ts (달력 유틸리티)
├── context/ (선택)
│   └── AppContext.tsx (전역 상태 관리)
└── App.tsx
```

#### B. TypeScript 타입 작성 (src/types/index.ts)
- User, Partner, Todo, Routine, TodoCompletion, DDay, Character 모두 정의
- (현재 지식정보.md의 2. 데이터 구조 복사)

#### C. localStorage 저장 전략 (Phase 1)
```typescript
// localStorage 키 구조
localStorage.keys = [
  "elhaejulhae_onboarding_progress",  // OnboardingProgress (E2: 온보딩 진행 상태)
  "elhaejulhae_user",                 // User (Step 4 완료 후 생성)
  "elhaejulhae_partner",              // Partner (선택)
  "elhaejulhae_dday",                 // DDay (Step 3 완료 후 생성)
  "elhaejulhae_character",            // Character (Step 4 완료 후 생성)
  "elhaejulhae_todos",                // Todo[]
  "elhaejulhae_routines",             // Routine[]
  "elhaejulhae_todoCompletions",      // TodoCompletion[]
]

// E2: 온보딩 진행 상태 저장 흐름
// 1. Step 1 완료 → onboarding_progress.step1 저장
// 2. Step 2 완료 → onboarding_progress.step2 저장
// 3. Step 3 완료 → onboarding_progress.step3 저장
// 4. Step 4 완료 → User, DDay, Character 생성 후 onboarding_progress 삭제
```

#### D. 핵심 로직 (Phase 1 구현 순서)
1. **온보딩**: Step 1~3 UI + 데이터 localStorage 저장
2. **MainPage**: Header (D-Day 카운트다운) + 월 뷰 캘린더
3. **투두 렌더링**: Todo (DB) + Routine (동적 생성)
4. **투두 완료**: ✓ 체크 → +10 EXP (즉시) → localStorage 동기화
5. **데이터 복구**: 앱 재실행 시 localStorage에서 복구

#### E. 주의사항
- ❌ Phase 1에서 firebase 미사용
- ❌ Phase 1에서 실시간 동기화 미구현
- ❌ Phase 1에서 짝꿍 알림 미구현
- ✅ Phase 1에서 로컬 상태 관리만 구현
- ✅ Routine의 동적 투두 생성 로직 정확하게 구현 (repeat: daily/weekly/biweekly/monthly)
- ✅ TodoCompletion 테이블로 완료 상태 추적 (Phase 2 Firebase 동기화 대비)

---

**문서 작성일**: 2026-09-13  
**기획서 근거**: `상세기획서.md` (v2 전체 스펙)  
**참고 프로젝트**: `ElhaeJulhae` v1 (Firebase 패턴, 데이터 스키마)
