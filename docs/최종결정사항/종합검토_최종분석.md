# 종합 검토 & 최종 분석

**작성일**: 2026-09-13  
**목적**: 21개 답변 종합 분석 + 데이터 구조 업데이트 + 시나리오 재검토

---

## 🎯 1단계: 주요 7개 문제 해결 현황

| 순번 | 문제 | 상태 | 해결 방식 |
|------|------|------|---------|
| **P1** | 데이터 저장소 | ✅ **해결** | localStorage (A-1) |
| **P3** | 반복 투두 설계 | ✅ **해결** | 투두↔루틴 분리 (C-3) — **새로운 데이터 타입 추가** |
| **P5** | 초대 코드 스펙 | ✅ **해결** | URL + 6자리 코드 하이브리드 (B-1) |
| **P6** | D-Day 공유 방식 | ✅ **해결** | 공유 D-Day (B-4) |
| **P7** | 재방문/인증 전략 | ✅ **해결** | 자동 복구 + 자동 재인증 (A-2, A-3) |
| **P9** | 실시간 알림 기술 | ⏳ **부분 명확** | Firebase Realtime/Listener 사용 (추가 구현 상세 필요) |
| **P12** | 루틴 투두 생성 | ✅ **해결** | 템플릿 방식 (D-Day 화면 제안) (C-2) |

**결론**: 7개 중 6개 완전 해결, 1개 부분 명확 ✅

---

## 🔄 2단계: 새로운 데이터 구조 (C-3 반영)

### 기획서의 원래 구조
```typescript
interface Todo {
  id: string
  text: string
  date: string (필수)
  category: 'common' | 'mine' | 'partner'
  repeat?: 'none' | 'daily' | 'weekly' | 'monthly'
  ...
}
```

### 🆕 변경된 구조: **투두 ↔ 루틴 분리**

#### 2.1 Todo (단일 날짜 투두)
```typescript
interface Todo {
  id: string
  text: string
  date: string                    // ⭐ 필수: 단일 날짜만
  category: 'common' | 'mine' | 'partner'
  assignedTo?: 'owner' | 'partner'
  completed: boolean
  completedAt?: string
  createdAt: string
  deletedAt?: string
  // ❌ repeat 필드 제거됨 (루틴으로 이전)
}
```

#### 2.2 🆕 Routine (반복 투두 = 새 타입!)
```typescript
interface Routine {
  id: string
  text: string
  category: 'common' | 'mine' | 'partner'
  assignedTo?: 'owner' | 'partner'
  
  // 반복 설정
  repeat: 'daily' | 'weekly' | 'biweekly' | 'monthly'
  
  // 시간 설정 (⭐ 새로운 필드!)
  time?: string                   // "06:00", "12:00" 형식
  times?: string[]                // ["06:00", "12:00", "18:00"] 다중 시간 가능
  
  // 유효 기간
  startDate: string               // 루틴 시작 날짜
  endDate?: string                // 루틴 종료 날짜 (없으면 무한)
  
  createdAt: string
  deletedAt?: string
}
```

#### 2.3 업데이트된 Character 구조
```typescript
interface Character {
  level: number
  currentExp: number              // 0 ~ 100
  totalExp: number                // 누적
  nextLevelExp: number            // 항상 100
}
```

**변경 영향도**:
- 🔴 **높음**: Database schema, 투두 리스트 렌더링, repeat 로직
- 🟡 **중간**: 투두 추가 폼, 데이터 마이그레이션 (기존 repeat → Routine 변환)

---

## ⚠️ 3단계: 새로운 논리적 모순 & 의사결정 포인트

### 발견 사항 1️⃣: 카테고리 분류 불일치
**문제**: B-5(투두 카테고리 사용자 선택형)와 C-2(루틴 템플릿 D-Day 제안)가 상충

```
B-5: 투두 추가 시 사용자가 "공통 / 내 것 / 짝꿍 것" 직접 선택
C-2: 루틴은 템플릿에서 제안 (자동 분류)

→ 루틴도 사용자 선택? 아니면 템플릿에서 정의된 카테고리?
  예: "영양제 복용" = "공통" (고정)인가?
      아니면 사용자가 "내 것"으로 변경 가능인가?
```

**현재 해석**: 루틴 템플릿은 기본 카테고리 제안하되, 추가 시 사용자가 변경 가능

---

### 발견 사항 2️⃣: E-1의 "자동으로 내 투두" 정의 모호
**문제**: 싱글→커플 모드 전환 시 기존 투두가 "내 투두"로 자동 분류된다는 것의 의미

```
Q: 싱글 모드에서 "공통" 카테고리가 없다면?
A: 싱글은 모든 투두가 "내 투두"로 간주됨 (또는 표시 안 함)

→ 커플 전환 후:
  기존 투두 (싱글에서는 구분 없음) 
  → 자동으로 "내 투두" (A 소유)로 변경
  → B의 투두 리스트에는 "짝꿍 투두"로 표시

✅ 논리적으로는 일관성 있음. 명시 필요.
```

---

### 발견 사항 3️⃣: 루틴의 시간 설정과 알림의 관계
**문제**: C-3에서 루틴이 시간 설정 가능인데, 이게 알림과 연결되는가?

```
예: "영양제 복용" 루틴
  - repeat: daily
  - time: ["06:00", "12:00"]
  
→ 매일 06:00과 12:00에 알림? 또는 단순 투두 표시?

D-4: "알림은 실시간만 (기록 없음)" 
→ 🔔 버튼을 짝꿍에게 누를 때만 알림?
   자동 시간 기반 알림은 없다?

❓ 루틴의 "time" 필드는 뭐에 쓰는가?
  A) 시간대별 자동 알림 (Phase 3+에서 구현)
  B) 단순 UI에서 시간 표시 (추후 자동화 대비)
```

**현재 해석 (권장)**: B) 단순 UI 표시 — Phase 1/2에서는 시간 미사용, Phase 3+ 자동 알림 대비

---

### 발견 사항 4️⃣: D-4 "알림 기록 없음"의 UX 문제
**문제**: 알림을 저장하지 않으면 중요한 알림을 놓칠 수 있다

```
시나리오: B가 앱을 닫았을 때 A가 🔔 알림 발송
→ D-4에서는 토스트 표시만 하고 기록 없음
→ B가 나중에 앱 재실행 해도 알림 내역 없음

❓ 괜찮은가?

권장: 실시간 토스트 표시는 하되, notification 테이블에는 저장
      (B가 나중에 "알림 센터"에서 확인 가능)
```

---

### 발견 사항 5️⃣: B-1 "URL + 코드 하이브리드" 구현 복잡도
**문제**: URL 초대와 코드 입력 2가지 방식 지원의 기술 복잡도

```
옵션 A: URL 클릭 시 앱에서 바로 처리
  - URL scheme: elhaejulhae://invite?code=ABC123
  - 또는 web link + deep link 병행
  - 웹 & 앱 모두 고려해야 함

옵션 B: 코드 수동 입력만 지원 (B-2로 단순화)
  - 7일 유효 코드만 사용
  - 입력 폼에서 수동으로 입력

현재 B-1 선택: A + B 병행 (URL 우선, 코드 폴백)
→ ⚠️ 구현 복잡도 높음 (Phase 2에서 고려 필요)
```

---

## ✅ 4단계: 앞뒤 맥락 일관성 검토

### 체크리스트
| 항목 | 검토 | 결과 |
|------|------|------|
| **A-1 ↔ A-2**: localStorage vs 초기화 선택 | 로컬에 저장하되, 사용자가 초기화 선택 가능 | ✅ 일관 |
| **A-3 ↔ Phase 2**: 자동 재인증 vs Firebase 토큰 | Firebase 토큰 만료 시 자동 재인증 (사용자 개입 없음) | ✅ 일관 |
| **B-1 ↔ B-2**: URL + 코드 ↔ 7일 유효기간 | 코드는 7일 유효, URL도 같은 코드 사용 | ✅ 일관 |
| **B-3 ↔ B-5**: 수락 모달 ↔ 투두 카테고리 | B 수락 후 A의 투두가 B에게 "짝꿍 투두"로 표시 | ✅ 일관 |
| **B-4 ↔ C-3**: 공유 D-Day ↔ 루틴 startDate | D-Day 기반 루틴의 startDate 자동 설정 | ✅ 일관 (명시 필요) |
| **B-5 ↔ E-1**: 사용자 선택형 ↔ 자동 분류 | 투두 추가: 사용자 선택 / 모드전환: 자동 분류 | ✅ 일관 |
| **C-2 ↔ C-3**: 템플릿 루틴 ↔ startDate~endDate | D-Day 화면의 루틴 제안 시 날짜 범위 설정 | ✅ 일관 (명시 필요) |
| **D-1 ↔ D-3**: 토스트 위치 ↔ 스와이프 | 화면 상단 플로팅 토스트, 스와이프 닫기 가능 | ✅ 일관 |
| **D-4 ↔ Phase 2/3**: 실시간만 ↔ 알림 저장 | 토스트 표시만 하되, DB 저장 고려 필요 | ⚠️ 명시 권장 |
| **F-1 ↔ Phase 1/2**: 즉시 +10 EXP ↔ Firebase 동기화 | Phase 1: 로컬 즉시 / Phase 2: Firebase 동기화 | ✅ 일관 |

**결론**: 대부분 일관성 있으나, **D-4와 C-3의 time 필드** 명시 권장 ⚠️

---

## 🎯 5단계: 재검토된 7개 사용자 시나리오

### 시나리오 1: 신규 유저 온보딩 (싱글 모드)

#### 흐름 ✅
```
1. OnboardingPage Step 1~3 완료
2. Step 4: 월 뷰 선택
3. MainPage 진입
   - Header: "배란일까지 7일" (F-3)
   - 월 뷰: 배란일(9-20) 강조
   - TodoList: 공통/내 투두/짝꿍 투두 탭
```

#### 새로운 검토 ✅
- ✅ 투두 추가는 월 뷰 날짜 클릭 (C-1)
- ✅ 로컬 localStorage 저장 (A-1)
- ⚠️ "짝꿍 투두" 탭: 싱글 모드인데 뭐가 표시되나? → **미지정**

**의사결정 필요 1️⃣**: 싱글 모드에서 "짝꿍 투두" 탭을 표시할지? 숨길지?

---

### 시나리오 2: 신규 유저 온보딩 (커플 모드 - 초대)

#### [A: 초대자] 흐름 ✅
```
1. OnboardingPage Step 1~3 완료
2. Step 2: "짝꿍과 함께" 선택
   - Partner 정보 입력: gender, birthDate (+ 이름? 연락처?)
   
   ❓ 의사결정 필요 2️⃣: Partner 구조에 name/phone/email 추가?
   
   - "초대 코드 생성" 또는 URL 생성 (B-1)
   - 초대 코드 표시 (예: ABC123)
   - URL 생성 (예: https://app.com/invite?code=ABC123)
   
3. Step 3~4 완료
4. MainPage: "짝꿍 대기중" 표시? (B가 아직 미연결)
```

#### [B: 초대받은자] 흐름 ✅
```
1. OnboardingPage Step 1 ~ 2
   - Step 2: "짝꿍과 함께" 선택
   
   옵션 A) URL 클릭: invite?code=ABC123 자동 입력 (B-1)
   옵션 B) 코드 수동 입력: ABC123 입력 필드
   
2. [확인] 클릭
   - 수락 모달 표시 (B-3)
   - "A와 연결하시겠어요?" 승인
   
3. B-3: 백그라운드에서
   - B의 User.role = 'partner'
   - A ↔ B의 room/connection 생성
   - A의 기존 투두가 B에게 "짝꿍 투두"로 표시
   
4. Step 3: D-Day 설정
   - ❓ 의사결정 필요 3️⃣: B도 같은 D-Day 입력? 또는 A의 D-Day 자동 적용?
   
   현재 B-4: 공유 D-Day → A와 동일 사용 (B도 입력?)
   
5. MainPage: A와 B가 동일 캘린더 뷰 (공유)
```

**의사결정 필요 2️⃣**: Partner 구조 확장 필요?
**의사결정 필요 3️⃣**: B의 D-Day 입력 필요? 또는 자동 적용?

---

### 시나리오 3: 투두 추가 및 완료

#### 흐름 (분리된 투두 vs 루틴) ✅
```
[단일 투두 추가]
1. 월 뷰 9-15일 클릭
2. 투두 추가 모달
   제목: "병원 방문"
   날짜: 2026-09-15 (자동 선택)
   카테고리: "공통" / "내 투두" / "짝꿍 투두" (사용자 선택) (B-5)
   [추가] 클릭
   
3. 로컬 localStorage 저장
4. 9-15 화면에 Todo 표시

[반복 루틴 추가]
1. 월 뷰 배란일(9-20) 클릭
   → D-Day 상세 화면
   
2. "루틴 투두" 제안 표시 (C-2)
   - 영양제 복용 (매일)
   - 운동 30분 (매일)
   - 스트레스 관리 (매일)
   
3. "영양제 복용" [추가] 클릭
   → Routine 생성
   ```
   {
     text: "영양제 복용",
     repeat: "daily",
     startDate: "2026-09-20",
     time: ["06:00", "12:00"], // C-3
     ...
   }
   ```
   
4. 루틴 생성 후, 매일(9-20부터) 날짜별 투두로 표시?
   
   ❓ 의사결정 필요 4️⃣: Routine을 어떻게 렌더링?
   - A) 루틴 테이블에만 저장, UI에서 동적 생성
   - B) 각 날짜마다 Todo 자동 생성 (DB에 9-20, 9-21, 9-22... 각각)

[투두 완료]
1. 9-15 "병원 방문" [✓] 체크
2. 즉시 +10 EXP (F-1)
3. Character.currentExp: 0 → 10 (로컬 업데이트)
4. localStorage 동기화
```

**의사결정 필요 4️⃣**: Routine 렌더링 방식 (동적 vs 사전 생성)?

---

### 시나리오 4: 짝꿍 실시간 알림

#### 흐름 ✅
```
[A: 알림 발송]
1. MainPage TodoList → "짝꿍 투두" 탭
2. "정액 채취 준비" 옆 🔔 클릭
3. Firebase로 전송
   {
     from: A_userId,
     to: B_userId,
     message: "A가 '정액 채취 준비' 확인해달라고 했어요! 🔔",
     timestamp: ...
   }

[B: 알림 수신]
4. 화면 상단에 토스트 플로팅 (D-1)
   "🔔 A가 '정액 채취 준비' 확인해달라고 했어요!"
   
5. 토스트 상태 (D-2, D-3)
   - 지속시간: 5초 (D-2)
   - 클릭: 투두로 이동 (Day 뷰)
   - 스와이프 4방향: 닫기
   
6. B가 Day 뷰에서 체크
   [✓] 정액 채취 준비
   
7. B의 화면: +10 EXP
8. Firebase에 동기화 (Phase 2)
   - A의 화면도 B의 완료 상태 실시간 반영 ✅
```

**명시된 부분**: D-1~D-5 모두 명확
**검토 결과**: ✅ 논리적 일관

---

### 시나리오 5: D-Day 기반 투두 추천

#### 흐름 ✅
```
[배란일(9-20) 클릭]
1. 캘린더 배란일 클릭 → D-Day 상세 화면
2. 루틴 템플릿 제안 (C-2)
   ```
   루틴 투두 (기본 제안):
   - 영양제 복용 (매일)
   - 운동 30분 (매일)
   - 스트레스 관리 (매일)
   
   이벤트 투두 (자연임신):
   - 배란 테스트기 사용
   - 기초체온 측정
   ```
   
3. "배란 테스트기 사용" [추가] 클릭
   → Todo 생성 (날짜: 9-20)
   
   ❓ 의사결정 필요 5️⃣: 이벤트 투두도 루틴? 아니면 일회 투두?
   
   현재 해석: 일회 투두 (배란 테스트기는 매일 아님)
   
4. "기초체온 측정"은?
   - C-4에서: "루틴의 날짜 범위" (startDate~endDate)
   - 배란일 -5 ~ +5일 동안 매일 측정?
   
   ❓ 의사결정 필요 6️⃣: 기초체온 같은 여러 날 투두 어떻게 표현?
   
   옵션 A) Routine으로: repeat=daily, startDate=9-15, endDate=9-25
   옵션 B) 여러 개 Todo: 9-15, 9-16, ..., 9-25 각각
   옵션 C) Todo에 dateRange 필드 추가
```

**의사결정 필요 5️⃣**: 이벤트 투두 (배란 테스트, 기초체온) 투두 vs 루틴 분류?
**의사결정 필요 6️⃣**: 여러 날 투두의 구현 방식?

---

### 시나리오 6: 모드 전환 (싱글 ↔ 커플)

#### 흐름 ✅
```
[A: 싱글 → 커플 전환]
1. MainPage Menu → "환경설정" → "모드 변경"
2. "짝꿍과 함께" 선택
3. Partner 정보 입력 (gender, birthDate, + name?)
4. 초대 코드 생성 (B-1)
5. 모드 전환 완료

[기존 데이터 처리]
6. 기존 투두 5개 (싱글 모드)
   → E-1: 자동으로 "내 투두"로 분류
   
   ✅ 해석: 싱글 모드는 "공통" 카테고리 없음 → 모두 "내 것"으로 간주
            전환 후 A의 "내 투두"로 유지
   
7. 기존 EXP 50 유지 ✅
8. 기존 루틴 5개?
   - 루틴도 동일하게 "내 루틴"으로 분류?

[B: 초대 코드 입력 후 연결]
9. B가 가입 후 초대 코드 입력
10. 수락 모달 (B-3) → 승인
11. A와 B 연결 완료
12. MainPage: 공유 캘린더 & 투두 리스트
    - A: 내 투두 5개 + 짝꿍 투두 (B가 추가한 것들)
    - B: 내 투두 0개 (아직 추가 안 함) + 짝꿍 투두 5개 (A가 전환 전 추가)
```

**검토 결과**: ✅ E-1 일관성 있음

---

### 시나리오 7: 앱 재방문 & 로그인

#### Phase 1 (Firebase 없음)
```
1. A가 앱 설치 후 온보딩 완료
2. 3일 후 앱 재실행
3. localStorage에서 userData 불러옴 (A-1)
4. MainPage 복구 (투두, EXP, 캐릭터)
5. 추가 로그인 불필요
```

#### Phase 2+ (Firebase 사용)
```
1. Firebase 익명 인증 토큰 확인
2. 토큰 유효?
   - YES: Firebase에서 최신 데이터 다운로드 → MainPage
   - NO: 자동 재인증 (A-3) → 새 토큰 발급 → 복구
   
3. 사용자는 로그인 프로세스 보지 않음 (백그라운드)
```

**검토 결과**: ✅ A-2, A-3과 일관

---

## 🚨 6단계: 최종 의사결정 필요 항목 (6개)

| 번호 | 항목 | 영향도 | 현황 |
|------|------|--------|------|
| **D1** | 싱글 모드에서 "짝꿍 투두" 탭 표시 여부 | 🟡 중간 | 미지정 |
| **D2** | Partner 구조 확장 (이름/연락처 추가) | 🟡 중간 | 기본값: 추가 (초대 시 필요) |
| **D3** | B의 D-Day 입력 vs 자동 적용 | 🟡 중간 | 기본값: A 동일 자동 적용 |
| **D4** | Routine 렌더링 방식 (동적 vs 사전 생성) | 🔴 높음 | 미지정 (성능 영향) |
| **D5** | 이벤트 투두 분류 (투두 vs 루틴) | 🟡 중간 | 기본값: 투두 (배란일 전후만) |
| **D6** | 여러 날 투두 구현 방식 (dateRange vs 루틴 vs 개별) | 🔴 높음 | 미지정 (기초체온 측정 등) |

---

## 📋 7단계: 데이터 구조 최종 버전 (업데이트된 지식정보.md용)

### 업데이트 필요 항목

#### 2.3 추가 필드: Partner 확장
```typescript
interface Partner {
  gender: 'female' | 'male'
  birthDate: string
  name?: string                   // 🆕 추가 (초대 시)
  phone?: string                  // 🆕 추가 (선택)
  email?: string                  // 🆕 추가 (선택)
}
```

#### 2.2 Routine 추가 설명
```typescript
interface Routine {
  // ... (위와 동일)
  
  // 사용 예
  // - 루틴 투두: "영양제 복용" (매일, 무한)
  // - 이벤트 루틴: "기초체온 측정" (D-Day 기간만)
  // - 시간대별: "영양제" (daily, 06:00+12:00)
}
```

#### 3.0 새로운 섹션: "투두 vs 루틴" 구분 원칙
```
투두 (Todo):
- 단일 날짜에 수행
- 예: "병원 방문" (2026-09-15)
- 반복 개념 없음
- 완료하면 사라짐

루틴 (Routine):
- 반복 날짜에 수행
- 예: "영양제 복용" (매일, 06:00+12:00)
- startDate ~ endDate 범위 지정 가능
- 렌더링: 동적 (각 날짜마다 표시) 또는 사전 생성
- 완료하면 다음 반복일에 자동 재생성
```

---

## ✅ 8단계: 최종 검토 결론

### 문제 해결 현황
- ✅ 7개 주요 문제 해결됨 (6개 완전, 1개 부분)
- ✅ 앞뒤 맥락 대부분 일관
- ⚠️ 6개 추가 의사결정 필요

### 새로운 데이터 구조
- ✅ 투두 ↔ 루틴 분리 (C-3 반영)
- ✅ Partner 확장 가능 (선택)
- ⚠️ Routine 렌더링 방식 미정 (성능 영향)

### 시나리오 검토 결과
- ✅ 1번: 싱글 온보딩 — 탭 표시 문제만 미정
- ✅ 2번: 커플 온보딩 — Partner 정보, D-Day 처리 미정
- ⚠️ 3번: 투두/루틴 추가 — 루틴 렌더링, 여러 날 투두 구현 미정
- ✅ 4번: 실시간 알림 — 완전히 명확
- ⚠️ 5번: D-Day 투두 — 이벤트 투두 분류, 여러 날 구현 미정
- ✅ 6번: 모드 전환 — 완전히 명확
- ✅ 7번: 재방문 — 완전히 명확

### 다음 단계
1. **6개 의사결정 항목** 확인/승인
2. **지식정보.md** 업데이트 (투두/루틴 분리, Partner 확장 반영)
3. **상세기획서.md** 보완 (필요 시)
4. **Phase 1 구현 착수**

---

**상태**: 🟢 **대부분 명확, 6개 항목만 최종 확인 필요**

(2026-09-19 기준)