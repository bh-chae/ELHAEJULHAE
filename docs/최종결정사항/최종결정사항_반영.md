# 최종 결정사항 반영 (E1~E10)

**작성일**: 2026-09-19  
**목적**: 사용자 최종 결정사항 10개 항목 정리 및 구현 가이드  
**상태**: ✅ 모든 항목 확정

---

## 📋 10개 의사결정 최종 결정 (E1~E10)

### E1: Step 4 "뷰 선택"의 Phase 배치 ✅
```
결정: 🔄 선택적 처리 (기본값: Month 자동 선택, 나중에 변경 가능)

구현:
- 온보딩 Step 4: 뷰 선택 제안 ("Month / Week / Day")
- Phase 1: Month 뷰만 구현 (기본값으로 자동 선택)
- Week/Day 뷰: Phase 4에서 추가 구현
- 사용자가 원하면 설정에서 나중에 뷰 변경 가능

변경 로직:
MainPage Menu → 환경설정 → 기본 뷰 선택
  → Month / Week (Phase 4+) / Day (Phase 4+)
```

---

### E2: 온보딩 완료 시 데이터 저장 시점 ✅
```
결정: 📝 Step별 저장 (Step 1,2,3 각각 저장 → Step 4 완료 후 최종 생성)

프로세스:
Step 1 완료:
  └─ OnboardingProgress.step1 저장 (성별, 생년월일, stage)
  
Step 2 완료:
  └─ OnboardingProgress.step2 저장 (모드, Partner 정보)
  
Step 3 완료:
  └─ OnboardingProgress.step3 저장 (D-Day 정보)
  
Step 4 완료 (또는 Skip):
  └─ 최종 생성: User, DDay, Character 한 번에 생성
  └─ onboarding_progress 삭제
  └─ MainPage로 이동

장점:
- 온보딩 도중 이탈했다가 재방문 시 진행 상태 복구 가능
- localStorage 용량 효율적 (최종 생성까지만 임시 저장)
- Phase 2에서 Firebase 동기화 시 데이터 일관성 유지
```

---

### E3: D-Day 카운트다운 표시 정밀도 ✅
```
결정: 📅 일수만 ("배란일까지 7일")

구현:
Header에 표시:
  "배란일까지 7일" (또는 "D-Day 7일 남음")
  
특징:
- 시간/분/초는 표시하지 않음
- 배터리 효율 고려 (초 단위 업데이트 불필요)
- 자정(00:00)에 한 번 업데이트

계산:
targetDate - today = D-Day
  (시간/분초는 무시, 일수만 계산)
```

---

### E4: 루틴 완료 시 EXP 지급 방식 ✅
```
결정: ⚡ 투두와 동일 (+10 EXP 매번)

구현:
- 투두 완료: +10 EXP (1회만)
- 루틴 완료 (일회): +10 EXP
- 루틴 완료 (반복): 매번 +10 EXP
  예: "영양제 복용" 매일 → 매일 +10 EXP

수동 계산:
투두 5개 완료 + 루틴(매일) 7일 완료 = 50 + 70 = 120 EXP
  → Lv.1 (100 EXP) → currentExp: 20
```

---

### E5: 월 뷰에서 투두 추가 UI ✅
```
결정: 📅 날짜 클릭 후 모달 표시

구현:
1. 월 뷰에서 특정 날짜 클릭
   └─ 투두 추가 모달 표시

2. 투두 추가 모달 구성:
   ├─ 제목 (필수)
   ├─ 날짜 (자동: 클릭한 날짜)
   ├─ 카테고리 (자동: 현재 탭 기반)
   │  └─ E6에서 처리
   └─ [추가] / [취소]

3. 플로팅 버튼은 Phase 4에서 추가 (주/일 뷰 구현 시)
```

---

### E6: 싱글 모드 탭 구성 ✅
```
결정: 📑 "공통 / 내 투두" 탭 2개 (짝꿍 투두 없음)

구현:
싱글 모드:
  ├─ 공통 탭 (category: "common")
  └─ 내 투두 탭 (category: "mine")
  
커플 모드:
  ├─ 공통 탭 (category: "common")
  ├─ 내 투두 탭 (category: "mine")
  └─ 짝꿍 투두 탭 (category: "partner")

특징:
- 탭 선택 → 투두 추가 시 category 자동 설정 (사용자 선택 불필요)
- UI: 탭별로 필터링된 투두 표시
```

---

### E7: 루틴 반복 계산 방식 (요일 독립 지정) ✅
```
결정: 📆 dayOfWeek 필드 추가 - startDate와 독립적으로 반복 요일 지정

데이터 구조:
Routine {
  id: "routine_001",
  text: "영양제 복용",
  repeat: "weekly",
  startDate: "2026-09-18" (수요일)
  dayOfWeek: 1 (월요일) // ← 핵심!
  ...
}

렌더링 로직:
- startDate가 수요일이어도
- dayOfWeek=1 (월요일)이면
- 매주 월요일에만 투두 동적 생성

사용 예:
startDate="2026-09-18" (수요일), repeat="weekly", dayOfWeek=1 (월요일)
  → 2026-09-23 (월)
  → 2026-09-30 (월)
  → ... (매주 월요일)

biweekly의 경우:
  startDate="2026-09-18", repeat="biweekly", dayOfWeek=2 (화요일)
  → 기준주: 화요일부터 시작
  → 2주마다 화요일 반복

특징:
- 사용자가 "매주 월요일" 같이 명확하게 지정 가능
- startDate는 "루틴 시작 시점", dayOfWeek는 "반복 패턴"
- Phase 1: dayOfWeek 필드만 추가, 반복 로직은 간단하게 구현
- Phase 2+: 복잡한 반복 규칙 추가 가능
```

---

### E8: D-Day 변경 시 기존 루틴 처리 ✅
```
결정: 📝 사용자 확인 모달 (자동 변경 vs 유지)

시나리오:
D-Day 변경: 2026-09-20 (배란일) → 2026-09-25 (배란일)

기존 루틴:
  - "기초체온 측정" (startDate~endDate: 09-15~09-20)
  - "배란 테스트기" (startDate~endDate: 09-18~09-22)

변경 프로세스:
1. 사용자가 "환경설정" → "D-Day 수정" → "2026-09-25" 입력
2. 시스템: 영향받는 루틴 2개 감지
3. 확인 모달 표시:
   ┌─────────────────────────────────────┐
   │ 📝 D-Day가 변경됩니다.              │
   │                                     │
   │ 다음 2개 루틴이 영향을 받습니다:    │
   │ • 기초체온 측정 (09-15~09-20)      │
   │ • 배란 테스트기 (09-18~09-22)      │
   │                                     │
   │ 이 루틴들의 endDate도                │
   │ 변경하시겠어요?                     │
   │                                     │
   │ [변경] [취소]                       │
   └─────────────────────────────────────┘
   
4a. [변경] 선택:
    - endDate를 새 D-Day 기준으로 자동 계산
    - "기초체온": 09-15~09-25 (기존 -5~0 → 새 -5~0)
    - "배란테스트": 09-18~09-25 (기존 -2~+2 → 새 -7~0)
    
4b. [취소] 선택:
    - D-Day 변경 안 함
    - 루틴 유지 (기존대로)

이유:
- 사용자가 수동으로 루틴을 커스텀했을 수 있음
- 확인 없이 자동 변경 시 데이터 손실 가능
- 명시적 사용자 의도 확인
```

---

### E9: Phase 1 재방문 시 로그인/복구 방식 ✅
```
결정: ❓ 복구 여부 확인 모달

상황:
- 앱 설치 후 온보딩 완료 (localStorage 저장)
- 3일 후 앱 재실행

흐름:
1. 앱 시작 → localStorage 확인
2. User 데이터 존재?
   ├─ YES: 저장된 데이터 복구
   │   ┌─────────────────────────────┐
   │   │ 📱 저장된 데이터가 있습니다. │
   │   │                             │
   │   │ 기존 데이터로 복구하시겠어요?│
   │   │                             │
   │   │ [복구] [새로 시작]          │
   │   └─────────────────────────────┘
   │   
   │   [복구] 선택 → MainPage로 이동
   │   [새로 시작] 선택 → 데이터 초기화 → OnboardingPage
   │
   └─ NO: OnboardingPage 진입

특징:
- Firebase 없이 localStorage만 사용 (Phase 1)
- 사용자가 명시적으로 복구/초기화 선택
- 초기화 시에도 위험 경고 표시
```

---

### E10: 투두/루틴 삭제 방식 (소프트 삭제) ✅
```
결정: 🗑️ 소프트 삭제 (deletedAt 필드, Phase 5에서 휴지통 기능)

구현:
투두/루틴 삭제:
  └─ deletedAt = 현재 시각 (완전 삭제 아님)

렌더링:
캘린더/리스트 표시:
  └─ WHERE deletedAt IS NULL (활성 투두만 표시)

휴지통 (Phase 5):
  └─ WHERE deletedAt IS NOT NULL (삭제된 투두 표시)
  └─ 복구 / 완전 삭제 옵션

코드 예:
// 캘린더에 표시할 때
const activeTodos = todos.filter(t => !t.deletedAt);

// 휴지통에 표시할 때 (Phase 5)
const deletedTodos = todos.filter(t => !!t.deletedAt);

이유:
- 사용자가 실수로 삭제할 가능성 높음
- 복구 기능 미리 준비 (Phase 5)
- DB에서 직접 삭제 불필요 (논리 삭제만)
```

---

### E1~E6 원본 의사결정 내용 (참고)

#### D4: Routine 렌더링 방식 (하이브리드) ✅

#### 개요
```
원칙: DB에는 정의만, UI에서 동적 생성
→ DB 최소화 + UI 유연성 최대화
```

#### 데이터 모델

##### 1. Routine (DB)
```typescript
interface Routine {
  id: string
  text: string
  category: 'common' | 'mine' | 'partner'
  repeat: 'daily' | 'weekly' | 'biweekly' | 'monthly'
  time?: string | string[]           // "06:00" 또는 ["06:00", "12:00"]
  startDate: string                   // 루틴 시작 날짜
  endDate?: string                    // 루틴 종료 날짜 (없으면 무한)
  createdAt: string
  deletedAt?: string
}
```

##### 2. 🆕 TodoCompletion (DB - 완료 추적)
```typescript
interface TodoCompletion {
  id: string
  routineId: string                   // 어느 루틴의 완료인지
  date: string                        // 완료 날짜
  completed: boolean
  completedAt?: string
  createdAt: string
}
```

##### 3. Todo (메모리 - 동적 생성)
```typescript
interface Todo {
  id: string                          // 동적 생성: routineId + date 조합
  text: string
  date: string                        // 해당 날짜
  category: 'common' | 'mine' | 'partner'
  completed: boolean                  // TodoCompletion에서 조회
  // ↑ 실제 데이터는 메모리상에만 존재
}
```

#### 렌더링 프로세스 (예: 2026-09-18 ~ 2026-09-25 캘린더 표시)

```
Step 1: Routine 정의 로드 (DB)
  └─ routine_001: "영양제 복용" (daily, 06:00+12:00, startDate=2026-09-01, endDate=무한)
  
Step 2: 캘린더 범위에서 동적 Todo 생성 (메모리)
  └─ 루틴의 repeat 규칙 적용
     repeat='daily' → 2026-09-18, 09-19, ..., 09-25 각각 1개씩 생성
     repeat='weekly' → 2026-09-18, 09-25 (예: 목요일 반복)
     repeat='biweekly' → 이전 주 기준으로 계산
     repeat='monthly' → 월 단위 계산
  
  생성된 Todo (메모리상):
  [
    { id: "routine_001_2026-09-18", text: "영양제 복용", date: "2026-09-18", completed: false },
    { id: "routine_001_2026-09-19", text: "영양제 복용", date: "2026-09-19", completed: false },
    ...
  ]

Step 3: TodoCompletion 확인 (DB)
  └─ DB 쿼리: WHERE routineId='routine_001' AND date BETWEEN '2026-09-18' AND '2026-09-25'
  └─ 완료된 항목 조회:
     [
       { routineId: "routine_001", date: "2026-09-18", completed: true },
       { routineId: "routine_001", date: "2026-09-20", completed: true }
     ]

Step 4: 완료 상태 병합 (UI)
  └─ Step 2의 동적 Todo + Step 3의 completion 상태 병합
  └─ UI 렌더링:
     2026-09-18: [✓] 영양제 복용
     2026-09-19: [ ] 영양제 복용
     2026-09-20: [✓] 영양제 복용
     2026-09-21: [ ] 영양제 복용
     ...
```

#### 사용자 완료 흐름

```
사용자가 [✓] 영양제 복용 (2026-09-18) 클릭
  ↓
1. UI: 메모리 Todo 상태 변경 (즉시 반영)
   { id: "routine_001_2026-09-18", completed: true }
   
2. DB: TodoCompletion 생성/업데이트 (로컬스토리지 또는 Firebase)
   {
     id: "completion_ABC123",
     routineId: "routine_001",
     date: "2026-09-18",
     completed: true,
     completedAt: "2026-09-18T10:30:00Z"
   }
   
3. EXP: +10 (즉시 반영) (F-1)
   Character.currentExp += 10

4. 다음 날 앱 재방문
   → 캐시/localStorage의 Routine 정의 로드
   → TodoCompletion 확인
   → 2026-09-18의 완료 상태 복구 ✅
```

#### 성능 최적화

```
✅ 장점:
- DB 크기 최소화: 루틴 정의만 저장, 개별 일자 데이터 안 저장
- UI 유연성: 범위 변경 시 동적 계산 가능
- 완료 추적: TodoCompletion만 저장 → 저장소 효율

⚠️ 주의:
- repeat 계산 로직 정확성 필수 (weekly, biweekly 경계 처리)
- 대량의 routines가 있을 때 동적 계산 성능 모니터링
- Phase 2 Firebase 동기화 시 TodoCompletion 구독 필수
```

---

### D5: 카테고리 분류 방식 (페이지 기반) ✅
```
결정: 사용자가 현재 보고 있는 "페이지 분류"에 따라 자동 결정

로직:
┌─ 공통 탭에서 투두 추가
│  └─ category: "common" (자동 설정)
│
├─ 내 투두 탭에서 투두 추가
│  └─ category: "mine" (자동 설정)
│
└─ 짝꿍 투두 탭에서 투두 추가 (커플 모드만)
   └─ category: "partner" (자동 설정)

구현:
- 투두 추가 모달: 사용자가 클릭한 탭 정보 전달
- 모달에서 category 필드 숨김 (자동 설정)
- 투두 생성 시 현재 탭 정보로 category 설정
```

---

### D6: D-Day 루틴 템플릿 (비활성화 상태 추천) ✅
```
결정: D-Day 화면에서 루틴을 "비활성화 상태"로 추천
      사용자가 선택하면 기본값이 설정된 상태로 제공

UI 표시:
┌────────────────────────────────────┐
│ 📋 추천 루틴 (배란일 기준)         │
├────────────────────────────────────┤
│ ☐ 영양제 복용                      │ ← 비활성화 (회색 톤)
│ ☐ 운동 30분                        │
│ ☐ 스트레스 관리                    │
│                                    │
│ ☐ 배란 테스트기 사용               │
│ ☐ 기초체온 측정                    │
└────────────────────────────────────┘

사용자 선택 흐름:
1. "영양제 복용" 체크박스 클릭
2. 루틴 편집 모달 열림 (기본값 입력된 상태)
   {
     text: "영양제 복용",
     repeat: "daily",
     startDate: 배란일,
     endDate: (선택),
     time: ["06:00", "12:00"] (기본값)
   }
3. 사용자가 필요시 수정 후 [저장]

구현 주의:
- ☑ 활성화된 루틴만 실제로 생성됨
- ☐ 비활성화 루틴: DB에 저장 안 됨
- 사용자가 개별 선택한 루틴만 Routine 테이블에 추가
```

---

## 📊 최종 데이터 구조 (E1~E10 반영)

### 변경 1: OnboardingProgress 추가 (E2)
```typescript
interface OnboardingProgress {
  id: string
  step1?: { gender, birthDate, stage, completedAt }
  step2?: { mode, partner?, completedAt }
  step3?: { type, targetDate, completedAt }
  step4?: { selectedView, completedAt }
  createdAt: string
  deletedAt?: string
}

// 용도: 온보딩 중단 후 재방문 시 진행 상태 복구
```

### 변경 2: Routine에 dayOfWeek 필드 추가 (E7)
```typescript
interface Routine {
  id: string
  text: string
  category: 'common' | 'mine' | 'partner'
  repeat: 'daily' | 'weekly' | 'biweekly' | 'monthly'
  
  // ← NEW (E7)
  dayOfWeek?: number  // 0-6 (0=일, 1=월, ..., 6=토)
  // startDate와 무관하게 지정된 요일에만 반복
  
  startDate: string
  endDate?: string
  time?: string | string[]
  
  createdAt: string
  deletedAt?: string  // 소프트 삭제 (E10)
}
```

### 변경 3: 투두/루틴 소프트 삭제 확대 (E10)
```typescript
interface Todo {
  id: string
  text: string
  date: string (필수)
  category: 'common' | 'mine' | 'partner'
  completed: boolean
  completedAt?: string
  createdAt: string
  deletedAt?: string  // ← 소프트 삭제 (E10)
}

// 렌더링 시
const activeTodos = todos.filter(t => !t.deletedAt);
const deletedTodos = todos.filter(t => !!t.deletedAt); // Phase 5 휴지통
```

### 변경 4: 상세 데이터 구조 유지
```typescript
// 기존 구조는 유지:
- Partner: gender, birthDate만
- Todo: 단일 날짜 (repeat 제거)
- Routine: 반복 투두
- TodoCompletion: 루틴 완료 추적
- RoutineTemplate: D-Day 추천 루틴
```

---

## 📋 최종 의사결정 요약

| 항목 | 결정사항 | 영향도 |
|------|---------|--------|
| **투두 vs 루틴** | 분리 유지 (Todo에서 repeat 제거, Routine 추가) | 🔴 높음 |
| **카테고리 분류** | 페이지 기반 자동 설정 (사용자의 현재 탭에서 결정) | 🔴 높음 |
| **루틴 템플릿** | D-Day에서 비활성화 상태로 추천, 사용자 선택 시 기본값 제공 | 🟡 중간 |

---

## 🎯 Phase 1 구현 체크리스트 (E1~E10 적용)

### 데이터 구조
- [x] OnboardingProgress (E2: 온보딩 진행 상태)
- [x] Todo (E5, E6: 단일 날짜, 페이지 기반 category, E10: deletedAt)
- [x] Routine (E7: dayOfWeek 필드 추가, E10: deletedAt)
- [x] TodoCompletion (완료 추적)
- [x] RoutineTemplate (D-Day 루틴 추천)
- [x] Character (EXP 시스템, E4: Routine도 +10)
- [x] User (gender, birthDate, stage, role)
- [x] Partner (gender, birthDate만)
- [x] DDay (D-Day 정의, E3: 일수만, E8: 변경 시 확인 모달)

### UI/UX
- [ ] OnboardingPage Step 1~4 (E1: Step 4는 Month 기본값, 나중 변경 가능)
- [ ] OnboardingProgress 저장/복구 UI (E2)
- [ ] MainPage 기본 레이아웃
- [ ] 월 뷰 캘린더 (E3: D-Day 강조, 일수 카운트다운)
- [ ] TodoList (E6: 공통/내 투두) ← 짝꿍 투두 없음 (싱글 모드)
- [ ] 투두 추가 모달 (E5: 월 뷰 날짜 클릭 후 표시)
- [ ] 투두 완료 (✓ 체크, E4: +10 EXP)
- [ ] 투두 삭제 (E10: 소프트 삭제 → deletedAt 설정)
- [ ] 재방문 시 복구 확인 모달 (E9)
- [ ] D-Day 변경 시 루틴 확인 모달 (E8)

### 로컬 저장소
- [ ] localStorage: OnboardingProgress (E2)
- [ ] localStorage: 투두/루틴/완료 추적
- [ ] 앱 재실행 시 데이터 복구 (E9)

### 알림 & 커플 (Phase 2+)
- [ ] Phase 2: Firebase 연동
- [ ] Phase 3: 실시간 알림 & 동기화

---

## ✅ 최종 준비 상태

### 지식정보.md 업데이트 완료
- [x] E1: Step 4 선택적 처리 (Month 기본값)
- [x] E2: OnboardingProgress 엔티티 추가
- [x] E3: D-Day 일수만 표시
- [x] E4: Routine EXP +10
- [x] E5: 투두 추가 모달 (날짜 클릭)
- [x] E6: 싱글 모드 탭 구성
- [x] E7: Routine에 dayOfWeek 필드 추가
- [x] E8: D-Day 변경 시 확인 모달
- [x] E9: 재방문 복구 모달
- [x] E10: 소프트 삭제 (deletedAt)

### 상세기획서.md 업데이트 필요
- [ ] E1~E10 의사결정 사항 추가 반영 (선택)

### 구현 준비
- [x] E1~E6: 기본 의사결정 확정
- [x] E7~E10: 추가 의사결정 확정
- [x] 모든 데이터 구조 최종화
- [x] 모든 의사결정 사항 문서화

**상태**: 🟢 **E1~E10 모든 의사결정 확정, 구현 준비 완료!**

